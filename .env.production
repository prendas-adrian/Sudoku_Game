# MongoDB Configuration (Render will provide via environment)
NODE_ENV=production

# Port (Render assigns this automatically)
PORT=8080

# MongoDB URI (Set via Render dashboard - connection string from MongoDB service)
# MONGODB_URI will be auto-injected by Render from the MongoDB service
